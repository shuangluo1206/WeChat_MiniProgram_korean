# 云开发配置指南

## Step 1: 开通云开发

### 1.1 在微信开发者工具中开通

1. **点击顶部菜单栏的"云开发"按钮**
   ```
   工具栏 → 云开发 → 开通
   ```

2. **填写环境信息**
   - 环境名称：`hancard-prod`（随便起）
   - 环境ID：自动生成（记住这个ID！）
   - 套餐：**免费版**（1000用户足够）

3. **点击"创建"**
   - 等待1-2分钟
   - 看到"开通成功"

4. **记下环境ID**
   ```
   格式：hancard-xxx-xxxxxx
   例如：hancard-2g4dwa-1234567
   ```
   **⚠️ 这个ID后面要用！**

---

## Step 2: 创建数据库集合（5分钟）

### 2.1 进入云开发控制台

```
点击"云开发" → 会打开云开发控制台网页
```

### 2.2 创建3个数据库集合

**点击左侧"数据库"，然后创建以下3个集合：**

#### 集合1: words（单词库）
```
集合名称：words
权限设置：所有用户可读，仅创建者可写
```

**添加第一条测试数据**：
```json
{
  "korean": "고양이",
  "pronunciation": "goyangyi",
  "chinese": "猫",
  "type": "noun",
  "level": "beginner",
  "imageUrl": "https://via.placeholder.com/400x400.png?text=猫",
  "audioUrl": "",
  "category": "动物"
}
```

#### 集合2: learning_records（学习记录）
```
集合名称：learning_records
权限设置：仅创建者可读写
```

**这个集合先不添加数据**（用户学习后自动生成）

#### 集合3: user_stats（用户统计）
```
集合名称：user_stats
权限设置：仅创建者可读写
```

**这个集合也先不添加数据**（用户首次学习时自动创建）

---

## Step 3: 上传云函数（5分钟）

### 3.1 找到云函数文件夹

在微信开发者工具的左侧目录树中：
```
cloudfunctions/
└── login/
    ├── index.js
    └── package.json
```

### 3.2 上传login云函数

1. **右键点击 `login` 文件夹**
2. **选择"上传并部署：云端安装依赖"**
3. 等待1-2分钟
4. 看到"上传成功"

### 3.3 验证上传成功

在云开发控制台：
```
点击左侧"云函数" → 看到 login 函数
```

---

## Step 4: 配置环境ID（2分钟）

### 4.1 找到project.config.json

在VS Code中打开这个文件。

### 4.2 添加云开发环境ID

找到文件中的 `"cloudfunctionRoot"` 这一行附近，添加：

```json
{
  "cloudfunctionRoot": "cloudfunctions/",
  "cloudbaseRoot": "cloudbaserc.json",
  "miniprogramRoot": "./",
  "setting": {
    ...
  }
}
```

**⚠️ 如果没有 `cloudbaseRoot`，不用加！**

**真正重要的是：在代码中初始化云开发时指定环境ID**

---

## Step 5: 初始化云开发（已完成）

你的 `app.js` 中已经有初始化代码：

```javascript
wx.cloud.init({
  traceUser: true,
});
```

**这段代码会自动使用默认环境（你刚创建的环境）**

---

## Step 6: 删除测试代码，启用真实数据

现在云开发已配置好，可以用真实数据了！

### 6.1 删除learn.js中的测试数据

打开 `pages/learn/learn.js`，找到这段：

```javascript
// ===== 测试模式：使用假数据 =====
// ... 一大段测试代码
// ===== 测试模式结束 =====
```

**删除这整段**（包括注释），恢复原来的云数据库调用。

---

## Step 7: 测试

### 7.1 重新编译

在微信开发者工具：
```
Cmd + Shift + R (清缓存重新编译)
```

### 7.2 应该看到

- 如果words集合有数据：显示单词
- 如果words集合是空的：显示"今日任务已完成"

---

## 🎯 快速验证清单

完成后，检查这些：

```
✅ 云开发控制台能打开
✅ 3个数据库集合已创建
✅ words集合有至少1条测试数据
✅ login云函数已上传
✅ 小程序能正常运行，不报错
```

---

## ⚠️ 常见问题

### Q1: 点"云开发"没反应
**A**: 确保你的AppID是真实的（不是测试号）

### Q2: 提示"云开发权限不足"
**A**:
1. 确保你是小程序管理员
2. 或者让管理员在后台给你开通云开发权限

### Q3: 上传云函数失败
**A**:
1. 检查网络连接
2. 尝试"上传并部署：云端安装依赖"
3. 如果还失败，手动在云开发控制台创建函数

### Q4: 数据读取失败
**A**:
1. 检查集合权限设置
2. 确保words集合有数据
3. 查看调试器Console的错误信息

---

## 📚 下一步

配置完云开发后，你可以：

1. **批量导入单词** → 参考 `素材准备指南.md`
2. **测试完整流程** → 学习、答题、打卡
3. **真机测试** → 扫码在手机上测试

---

**现在开始配置吧！**

按步骤来，15分钟搞定！遇到问题随时问我 🚀
